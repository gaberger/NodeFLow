


//Constructor
function NodeSwitch() {
    if (false === (this instanceof NodeSwitch)) {
        return new NodeSwitch
    }
}

util.inherits(NodeSwitch, events.EventEmitter);


NodeFlow.prototype.doL2Learning = function(dpid, inport, packet) {
    ofl.sendOFMessage(socket, buffer)

}                        